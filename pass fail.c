#include <stdio.h>
int main(void)
{
                int num;
                printf("Enter any numbers: ");
                scanf("%d",&num);

                if(num>=33)
                                printf("pass");
                else
                                printf("fail");

                return 0;
}
